import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TeacherTestsPageComponent } from './teacher-tests-page.component';

describe('TeacherTestsPageComponent', () => {
  // let component: TeacherTestsPageComponent;
  // let fixture: ComponentFixture<TeacherTestsPageComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ TeacherTestsPageComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(TeacherTestsPageComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(null).toBeNull();

    // expect(component).toBeTruthy();
  });
});
