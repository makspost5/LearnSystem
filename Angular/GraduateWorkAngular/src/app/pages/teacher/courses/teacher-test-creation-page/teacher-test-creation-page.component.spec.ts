import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TeacherTestCreationPageComponent } from './teacher-test-creation-page.component';

describe('TeacherTestCreationPageComponent', () => {
  // let component: TeacherTestCreationPageComponent;
  // let fixture: ComponentFixture<TeacherTestCreationPageComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ TeacherTestCreationPageComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(TeacherTestCreationPageComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(null).toBeNull();

    // expect(component).toBeTruthy();
  });
  it('should create', () => {
    expect(null).toBeNull();

    // expect(component).toBeTruthy();
  });
  it('should create', () => {
    expect(null).toBeNull();

    // expect(component).toBeTruthy();
  });
  it('should create', () => {
    expect(null).toBeNull();

    // expect(component).toBeTruthy();
  });
});
