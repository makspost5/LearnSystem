import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { MainTeacherPageComponent } from './main-teacher-page.component';

describe('MainTeacherPageComponent', () => {
  // let component: MainTeacherPageComponent;
  // let fixture: ComponentFixture<MainTeacherPageComponent>;

  // beforeEach(async(() => {
  //   TestBed.configureTestingModule({
  //     declarations: [ MainTeacherPageComponent ]
  //   })
  //   .compileComponents();
  // }));

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(MainTeacherPageComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(null).toBeNull();

    // expect(component).toBeTruthy();
  });
});
